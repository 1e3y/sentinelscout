schema_version: 1
fixture_id: visible-surface
scope:
  root: bench.example
  include_subdomains: true
  exclusions: []
default_response_headers:
  Strict-Transport-Security: max-age=31536000
sites:
  - hostname: bench.example
    path: /
    html: bench.example/index.html
  - hostname: www.bench.example
    path: /
    html: www.bench.example/index.html
    skip_default_headers: true
  - hostname: staging.bench.example
    path: /
    html: staging.bench.example/index.html
  - hostname: admin.bench.example
    path: /
    html: admin.bench.example/index.html
  - hostname: login.bench.example
    path: /login
    html: login.bench.example/index.html
  - hostname: jenkins.bench.example
    path: /
    html: jenkins.bench.example/index.html
assets:
  - hostname: bench.example
    reachable: true
  - hostname: www.bench.example
    reachable: true
  - hostname: staging.bench.example
    reachable: true
  - hostname: admin.bench.example
    reachable: true
  - hostname: login.bench.example
    reachable: true
  - hostname: jenkins.bench.example
    reachable: true
# Fixture root is bench.example (RFC 2606 reserved). Labels "bench" and "example"
# are not Scout staging/dev markers, so the namespace itself must not emit
# staging_dev_exposed.
candidates:
  - id: staging-exposed
    hostname: staging.bench.example
    candidate_type: staging_dev_exposed
    expected: present
    desirable: true
    validation: supported
  - id: admin-exposed
    hostname: admin.bench.example
    candidate_type: exposed_admin_interface
    expected: present
    desirable: true
    validation: supported
  - id: login-auth
    hostname: login.bench.example
    candidate_type: auth_surface_observed
    expected: present
    desirable: true
    validation: supported
  - id: jenkins-sensitive
    hostname: jenkins.bench.example
    candidate_type: sensitive_service_exposed
    expected: present
    desirable: true
    validation: supported
  - id: jenkins-admin
    hostname: jenkins.bench.example
    candidate_type: exposed_admin_interface
    expected: present
    desirable: true
    validation: supported
  - id: www-missing-hsts
    hostname: www.bench.example
    candidate_type: security_header_observation
    expected: present
    desirable: true
    validation: supported
not_candidates:
  - hostname: bench.example
    candidate_type: staging_dev_exposed
  - hostname: www.bench.example
    candidate_type: staging_dev_exposed
  - hostname: www.bench.example
    candidate_type: exposed_admin_interface
  - hostname: www.bench.example
    candidate_type: auth_surface_observed
  - hostname: bench.example
    candidate_type: exposed_admin_interface
  - hostname: admin.bench.example
    candidate_type: staging_dev_exposed
  - hostname: login.bench.example
    candidate_type: staging_dev_exposed
  - hostname: jenkins.bench.example
    candidate_type: staging_dev_exposed
  - hostname: bench.example
    candidate_type: security_header_observation
  - hostname: staging.bench.example
    candidate_type: security_header_observation
  - hostname: admin.bench.example
    candidate_type: security_header_observation
  - hostname: login.bench.example
    candidate_type: security_header_observation
  - hostname: jenkins.bench.example
    candidate_type: security_header_observation
overlaps:
  - hostname: jenkins.bench.example
    candidate_types:
      - sensitive_service_exposed
      - exposed_admin_interface
    note: Expected overlap for v1 — both current deterministic rules match.
known_misses: []
retest: []
